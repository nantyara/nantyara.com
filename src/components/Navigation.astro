---
interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;

const menuItems = [
  { href: '/', label: '今後の予定', icon: '📅' },
  { href: '/past', label: 'これまでの予定', icon: '📜' },
  { href: '/releases', label: '音源', icon: '🎵' },
  { href: '/members', label: 'メンバー', icon: '👥' },
  { href: '/history', label: '沿革', icon: '📖' },
  { href: '/gallery', label: 'ギャラリー', icon: '📸' },
  { href: '/links', label: 'リンク', icon: '🔗' },
];
---

<nav class="navigation">
  {menuItems.map(item => (
    <div class={`nav-item ${currentPath === item.href ? 'active' : ''}`}>
      {currentPath === item.href ? (
        <span class="nav-button current">
          <span class="icon">{item.icon}</span>
          <span class="label">{item.label}</span>
        </span>
      ) : (
        <a href={item.href} class="nav-button">
          <span class="icon">{item.icon}</span>
          <span class="label">{item.label}</span>
        </a>
      )}
    </div>
  ))}
</nav>

<style>
  .navigation {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 12px;
    margin: 20px 0;
    padding: 0;
  }

  .nav-item {
    flex: 0 0 auto;
  }

  .nav-button {
    display: flex;
    align-items: center;
    gap: 8px;
    min-height: 44px;
    min-width: 120px;
    padding: 12px 20px;
    background: linear-gradient(135deg, var(--retro-purple) 0%, var(--retro-pink) 100%);
    color: white;
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 600;
    border-radius: 8px;
    border: 2px solid rgba(255, 255, 255, 0.2);
    box-shadow:
      0 4px 0 rgba(0, 0, 0, 0.3),
      0 0 15px rgba(255, 110, 199, 0.4);
    transition: all 0.3s ease;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .nav-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    transition: left 0.5s ease;
  }

  .nav-button:hover::before {
    left: 100%;
  }

  .nav-button:hover {
    transform: translateY(-2px);
    box-shadow:
      0 6px 0 rgba(0, 0, 0, 0.3),
      0 0 25px rgba(255, 110, 199, 0.6),
      0 0 35px rgba(0, 255, 255, 0.3);
    border-color: rgba(255, 255, 255, 0.4);
  }

  .nav-button:active {
    transform: translateY(0);
    box-shadow:
      0 2px 0 rgba(0, 0, 0, 0.3),
      0 0 10px rgba(255, 110, 199, 0.4);
  }

  .nav-button.current {
    background: linear-gradient(135deg, var(--retro-cyan) 0%, var(--retro-purple) 100%);
    box-shadow:
      0 4px 0 rgba(0, 0, 0, 0.3),
      0 0 25px rgba(0, 255, 255, 0.6),
      inset 0 0 20px rgba(255, 255, 255, 0.2);
    animation: pulse 2s infinite;
    cursor: default;
  }

  @keyframes pulse {
    0%, 100% {
      box-shadow:
        0 4px 0 rgba(0, 0, 0, 0.3),
        0 0 25px rgba(0, 255, 255, 0.6),
        inset 0 0 20px rgba(255, 255, 255, 0.2);
    }
    50% {
      box-shadow:
        0 4px 0 rgba(0, 0, 0, 0.3),
        0 0 35px rgba(0, 255, 255, 0.8),
        0 0 45px rgba(255, 110, 199, 0.4),
        inset 0 0 20px rgba(255, 255, 255, 0.2);
    }
  }

  .icon {
    font-size: 1.2rem;
    display: inline-block;
  }

  .label {
    display: inline-block;
    white-space: nowrap;
  }

  /* レスポンシブ対応 */
  @media (max-width: 768px) {
    .navigation {
      gap: 10px;
      margin: 15px 0;
    }

    .nav-button {
      min-width: 100px;
      padding: 10px 16px;
      font-size: 0.85rem;
    }

    .icon {
      font-size: 1.1rem;
    }
  }

  @media (max-width: 480px) {
    .navigation {
      gap: 8px;
      margin: 10px 0;
    }

    .nav-button {
      min-width: auto;
      padding: 10px 14px;
      font-size: 0.8rem;
      gap: 6px;
    }

    .icon {
      font-size: 1rem;
    }
  }

  /* タッチデバイス向けタップ領域拡張 */
  @media (hover: none) and (pointer: coarse) {
    .nav-button {
      min-height: 48px;
      padding: 14px 22px;
    }
  }

  /* アニメーション無効化（アクセシビリティ） */
  @media (prefers-reduced-motion: reduce) {
    .nav-button,
    .nav-button::before,
    .nav-button.current {
      animation: none !important;
      transition: none !important;
    }

    .nav-button:hover {
      transform: none;
    }
  }
</style>
